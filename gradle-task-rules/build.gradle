defaultTasks 'hello-MiniMe'

def pattern='hello'
tasks.addRule("Pattern: $pattern<message>") { String taskName ->
    if (taskName.startsWith(pattern)) {
        task(taskName) << {
            println "Hello ${taskName - pattern}"
        }
    }
}

task printIt(){
	ext.variable='done'
	doLast{
		println variable
	}
}

tasks['hello-MiniMe'].dependsOn printIt
